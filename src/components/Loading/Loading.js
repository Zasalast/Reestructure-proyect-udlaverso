import React from "react";

function Loading() {
  return (
    <div class="loadingSpinner">
      <span class="loadingSpinner-inner"></span>
      <span class="loadingSpinner-inner"></span>
      <span class="loadingSpinner-inner"></span>
      <span class="loadingSpinner-inner"></span>
    </div>
  );
}

export { Loading };
